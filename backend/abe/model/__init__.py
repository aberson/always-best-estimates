"""abe.model — the pluggable WorldModel forecaster layer.

Modules:
- ``base``: the frozen ``WorldModel`` Protocol + ``Forecast`` + ``EWMABaseline``.
- ``jepa`` / ``train``: minimal JEPA behind a config toggle (arrive in Step 13).
"""
